[
  {"split": "train", "should_trigger": true,
   "query": "test_checkout_applies_discount fails on CI but passes on my machine every time. Figure out what's going on."},
  {"split": "train", "should_trigger": true,
   "query": "Users are getting logged out at random. Sessions are supposed to last 30 days. No pattern I can see in the logs."},
  {"split": "train", "should_trigger": true,
   "query": "We're seeing `TypeError: cannot read property 'id' of undefined` in src/orders/summary.tsx in production. Fix it."},
  {"split": "train", "should_trigger": true,
   "query": "the nightly sync job silently drops about 3% of records. counts don't match. no errors in the log at all"},
  {"split": "train", "should_trigger": true,
   "query": "This test is flaky — passes maybe 4 times out of 5. Can you just add a retry so CI stops going red?"},
  {"split": "validation", "should_trigger": true,
   "query": "Memory climbs to about 4GB over roughly six hours and then the pod gets OOM-killed. Node service, api/worker.js is my suspicion."},
  {"split": "validation", "should_trigger": true,
   "query": "After the upgrade last Thursday, PDF export produces blank pages for some invoices but not others."},
  {"split": "validation", "should_trigger": true,
   "query": "Why does GET /api/search return 200 with an empty array when the query has an apostrophe in it?"},
  {"split": "train", "should_trigger": true,
   "query": "Tests pass locally and fail in CI about one run in five. No idea what differs between them."},
  {"split": "validation", "should_trigger": true,
   "query": "the 404 handler fires for /ping even though route() definitely registered it. find out why"},

  {"split": "train", "should_trigger": false, "expect": "post-mortem",
   "query": "We know what broke — a null check was missing in the webhook handler and we've already shipped the fix. What I want to understand is why it took four days to notice."},
  {"split": "train", "should_trigger": false, "expect": "verification-strategy",
   "query": "Bugs keep escaping to production. I don't want to chase this one, I want to fix how we catch them in general."},
  {"split": "train", "should_trigger": false, "expect": "none",
   "query": "Walk me through how a Python debugger works under the hood — what does sys.settrace actually do?"},
  {"split": "train", "should_trigger": false, "expect": "test-driven-development",
   "query": "Add the discount-stacking rule to the pricing engine: percentage discounts apply before fixed-amount ones."},
  {"split": "train", "should_trigger": false, "expect": "none",
   "query": "The build fails with `command not found: pnpm`. I just haven't installed it on this laptop yet."},
  {"split": "validation", "should_trigger": false, "expect": "requesting-code-review",
   "query": "I fixed the race condition in the queue consumer and the tests are green. Can someone look at the change before it goes in?"},
  {"split": "validation", "should_trigger": false, "expect": "complexity-audit",
   "query": "src/legacy/ is a minefield and every bug we hit lives in there. Audit it and tell me what's actually wrong with the design."},
  {"split": "validation", "should_trigger": false, "expect": "security-audits",
   "query": "Someone reported they can see another tenant's invoices by changing the id in the URL. Look at the authorization on that endpoint."},
  {"split": "train", "should_trigger": false, "expect": "post-mortem",
   "query": "We know exactly what broke and why, technically. The question is how it got past review and staging."},
  {"split": "validation", "should_trigger": false, "expect": "verification-before-completion",
   "query": "Nothing's broken — I just want confirmation that the fix I already made is genuinely done and passing."}
]
